<h1>Lista de Postagens</h1>
    {{#each posts}}
        <div>
            <h2>{{this.titulo}}</h2>
            <p>{{this.conteudo}}</p>
            <a href="/deletar/{{dataValues.id}}"><button>Deletar</button></a>
        </div>
    {{/each}}
  